Mono chip for tech/skill tags. `variant`: `default` (outline), `solid`, `accent`, `ink` (dark bg). `interactive` adds hover + button role.

```jsx
<Tag>roadmapping</Tag>
<Tag variant="accent">agile</Tag>
<Tag variant="ink">jira</Tag>
```
