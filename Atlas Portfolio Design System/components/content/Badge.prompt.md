Uppercase pill badge for project-type / category labels. `variant`: `accent` (amber), `muted`, `outline`, `ink`. Optional leading `dot`.

```jsx
<Badge variant="accent">Platform</Badge>
<Badge variant="outline" dot>0 → 1</Badge>
```
